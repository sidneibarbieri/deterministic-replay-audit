"""Exploratory experiments over the deterministic decision engine.

These modules are pure and reproducible. They do not fetch data or mutate state;
they sweep parameters over the existing scoring and deployment engines so that
findings can be observed, plotted, and regression-tested. The accompanying
driver (scripts/run_experiments.py) renders figures and JSON for the paper.
"""
